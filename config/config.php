<?php

error_reporting(E_ALL);
ini_set('display_errors', 0);

abstract class Config {
	
	// These variables must always be set
	
	
	/* These variables are custom set by you and can 
	 * be accessed through your controller
	 */
}
